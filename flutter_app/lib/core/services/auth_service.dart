//todas as chamadas relacionadas a login, signup, recuperação de senha, verificar OTP, reset de senha.
class AuthService {
  /// TODO(BACKEND): fazer logout real
  Future<void> signOut() async {
    await Future.delayed(const Duration(milliseconds: 150));
  }
}
